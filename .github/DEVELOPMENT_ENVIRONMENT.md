# FrankiFlow development environment

This file keeps the long-lived `develop` branch distinct from `main` so Netlify can maintain a development Deploy Preview.

Do not merge the development-environment pull request into `main`; application changes should be promoted separately after testing.
